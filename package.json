{
  "name": "shipmondo-for-woocommerce",
  "version": "5.0.0",
  "description": "Shipmondo for WooCommerce",
  "scripts": {
    "prod": "npm run build",
    "production": "npm run build",
    "build": "wp-scripts build --output-path=public/build/blocks --config=webpack.config.js",
    "watch": "wp-scripts start --output-path=public/build/blocks --config=webpack.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@woocommerce/dependency-extraction-webpack-plugin": "^3.0.0",
    "@woocommerce/eslint-plugin": "^2.2.0",
    "@woocommerce/tracks": "^1.3.0",
    "@wordpress/scripts": "^28.1.0",
    "eslint-plugin-jsdoc": "^48.4"
  },
  "dependencies": {
    "@vis.gl/react-google-maps": "^0.9.0"
  }
}
